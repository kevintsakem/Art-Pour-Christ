# ================================================================
# PROFIL PRODUCTION — activer avec : SPRING_PROFILES_ACTIVE=prod
# Toutes les variables ${VAR} sont OBLIGATOIRES en production
# ================================================================

# ==============================
# SERVER
# ==============================
server.address=0.0.0.0
server.port=${SERVER_PORT:${PORT:3001}}
spring.jpa.open-in-view=false

# ==============================
# DATABASE — PostgreSQL
# ==============================
# Format : jdbc:postgresql://HOST:PORT/DATABASE
spring.datasource.url=${DATABASE_URL}
spring.datasource.driver-class-name=org.postgresql.Driver
spring.datasource.username=${DATABASE_USERNAME}
spring.datasource.password=${DATABASE_PASSWORD}

spring.jpa.database-platform=org.hibernate.dialect.PostgreSQLDialect
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=false

# Pool de connexions HikariCP
# Garantit le bon fonctionnement peu importe le nombre d'utilisateurs
spring.datasource.hikari.maximum-pool-size=10
spring.datasource.hikari.minimum-idle=2
spring.datasource.hikari.connection-timeout=20000
spring.datasource.hikari.idle-timeout=300000
spring.datasource.hikari.max-lifetime=1200000

# ==============================
# H2 — DÉSACTIVÉ EN PRODUCTION
# ==============================
spring.h2.console.enabled=false

# ==============================
# JWT
# ==============================
jwt.secret=${JWT_SECRET}
# 1 heure en production (plus sécurisé que 24h)
jwt.expiration=${JWT_EXPIRATION:3600000}

# ==============================
# FILE UPLOAD — Cloudinary
# ==============================
# Définir CLOUDINARY_URL dans Railway : cloudinary://api_key:api_secret@cloud_name

# ==============================
# CORS — URL de ton frontend Vercel
# ==============================
app.cors.allowed-origins=${CORS_ORIGINS}

# ==============================
# ADMIN PAR DÉFAUT
# ==============================
app.admin.email=${ADMIN_EMAIL}
app.admin.password=${ADMIN_PASSWORD}

# ==============================
# MAIL — Gmail SMTP (ou autre fournisseur)
# ==============================
# Pour Gmail : activer "Mots de passe d'application" dans les paramètres du compte Google
# MAIL_USERNAME = adresse Gmail expéditrice
# MAIL_PASSWORD = mot de passe d'application (16 caractères)
# MAIL_FROM     = adresse affichée comme expéditeur (doit correspondre à MAIL_USERNAME)
spring.mail.host=${MAIL_HOST:smtp.gmail.com}
spring.mail.port=${MAIL_PORT:587}
spring.mail.username=${MAIL_USERNAME}
spring.mail.password=${MAIL_PASSWORD}
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true
spring.mail.test-connection=false
app.contact.from=${MAIL_FROM:noreply@artpourchrist.com}

# ==============================
# LOGGING — réduit en production
# ==============================
logging.level.root=WARN
logging.level.com.artpourchrist=INFO
logging.level.org.springframework.security=WARN
